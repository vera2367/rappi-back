package com.esteban.rappiback.repositorys;

import com.esteban.rappiback.entitys.ValoracionRappitendero;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ValoracionRappitenderoRepository extends JpaRepository<ValoracionRappitendero, String> {
}